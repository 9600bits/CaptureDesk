<Window Style="{StaticResource AppWindowStyle}" x:Class="CaptureDesk.App.PinWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="贴图" Width="420" Height="300" MinWidth="140" MinHeight="100" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False" ResizeMode="CanResizeWithGrip">
    <Border x:Name="Frame" Margin="6" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="1" CornerRadius="6"><Grid><Image x:Name="PinnedImage" Stretch="Uniform" MouseLeftButtonDown="Image_MouseLeftButtonDown" /><Border x:Name="Toolbar" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="1" CornerRadius="6" Padding="3" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8" Opacity="0"><StackPanel Orientation="Horizontal"><Button Style="{StaticResource VectorToolButton}" x:Name="TopmostButton" Content="{StaticResource PinIcon}" ToolTip="置顶" AutomationProperties.Name="切换置顶" Click="Topmost_Click" /><Button Style="{StaticResource VectorToolButton}" Content="{StaticResource OpacityIcon}" ToolTip="透明度" AutomationProperties.Name="切换透明度" Click="Opacity_Click" /><Button Style="{StaticResource VectorToolButton}" Content="{StaticResource CopyIcon}" ToolTip="复制" AutomationProperties.Name="复制贴图" Click="Copy_Click" /><Button Style="{StaticResource VectorToolButton}" Content="{StaticResource CloseIcon}" ToolTip="关闭" AutomationProperties.Name="关闭贴图" Click="Close_Click" /></StackPanel></Border></Grid></Border>
</Window>




