<Window Style="{StaticResource AppWindowStyle}" x:Class="CaptureDesk.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CaptureDesk.App"
        Title="CaptureDesk" Width="620" Height="420" MinWidth="560" MinHeight="400"
        Background="{DynamicResource CanvasBrush}" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="82"/><RowDefinition Height="*"/><RowDefinition Height="38"/></Grid.RowDefinitions>
        <Grid Margin="28,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="40" Height="40" Background="#F5F6F2" CornerRadius="5">
                    <Image Source="Assets/Brand/CaptureDesk.png" Margin="3" RenderOptions.BitmapScalingMode="HighQuality"/>
                </Border>
                <TextBlock Text="CaptureDesk" FontSize="20" FontWeight="520" VerticalAlignment="Center" Margin="12,0,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource VectorToolButton}" Content="{StaticResource SettingsIcon}" ToolTip="设置" AutomationProperties.Name="打开设置" Click="Settings_Click"/>
        </Grid>
        <Grid Grid.Row="1" Margin="28,8,28,20">
            <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="1"/><RowDefinition Height="62"/><RowDefinition Height="42"/></Grid.RowDefinitions>
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="截取此刻" FontSize="26" FontWeight="520"/>
                    <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                        <local:LucideIcon Data="{StaticResource KeyboardIcon}" Width="18" Height="18" Foreground="{DynamicResource MutedBrush}" Margin="0,0,8,0"/>
                        <TextBlock Text="Ctrl + Shift + A" FontSize="13" Foreground="{DynamicResource MutedBrush}"/>
                    </StackPanel>
                </StackPanel>
                <Button Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="开始截图" Height="44" VerticalAlignment="Center" AutomationProperties.Name="开始区域截图" Click="Capture_Click"/>
            </Grid>
            <Border Grid.Row="1" Background="{DynamicResource LineBrush}"/>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="16"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Button Style="{StaticResource TextActionButton}" Padding="10,0" AutomationProperties.Name="从剪贴板创建贴图" Click="Clipboard_Click">
                    <StackPanel Orientation="Horizontal"><local:LucideIcon Data="{StaticResource ClipboardIcon}" Width="20" Height="20" Margin="0,0,12,0"/><TextBlock Text="剪贴板贴图" VerticalAlignment="Center"/></StackPanel>
                </Button>
                <Button Grid.Column="2" Style="{StaticResource TextActionButton}" Padding="10,0" AutomationProperties.Name="打开截图历史" Click="History_Click">
                    <StackPanel Orientation="Horizontal"><local:LucideIcon Data="{StaticResource HistoryIcon}" Width="20" Height="20" Margin="0,0,12,0"/><TextBlock Text="截图历史" VerticalAlignment="Center"/></StackPanel>
                </Button>
            </Grid>
            <Button Grid.Row="3" Style="{StaticResource TextActionButton}" Height="34" Padding="10,0" HorizontalAlignment="Left" Click="RecoverRecording_Click">
                <StackPanel Orientation="Horizontal"><local:LucideIcon Data="{StaticResource VideoIcon}" Foreground="{DynamicResource MutedBrush}" Width="16" Height="16" Margin="0,0,16,0"/><TextBlock Text="恢复录制…" FontSize="12" Foreground="{DynamicResource MutedBrush}" VerticalAlignment="Center"/></StackPanel>
            </Button>
        </Grid>
        <Border Grid.Row="2" Background="{DynamicResource SurfaceMutedBrush}">
            <DockPanel Margin="28,0" LastChildFill="False">
                <TextBlock x:Name="ShortcutStatus" Text="Ctrl + Shift + A 随时截图" FontSize="12" Foreground="{DynamicResource MutedBrush}" VerticalAlignment="Center"/>
                <TextBlock DockPanel.Dock="Right" Text="0.4" FontSize="12" Foreground="{DynamicResource MutedBrush}" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
